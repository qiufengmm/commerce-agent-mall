<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.portal.dao.PortalOrderDao">
    <resultMap id="orderDetailMap" type="com.macro.mall.portal.domain.OmsOrderDetail"
               extends="com.macro.mall.mapper.OmsOrderMapper.BaseResultMap">
        <collection property="orderItemList" columnPrefix="ot_"
                    resultMap="com.macro.mall.mapper.OmsOrderItemMapper.BaseResultMap">
        </collection>
    </resultMap>
    <select id="getDetail" resultMap="orderDetailMap">
        SELECT
            o.id,
            o.order_sn,
            o.coupon_id,
            o.integration,
            o.use_integration,
            o.member_id,
            ot.id ot_id,
            ot.product_name ot_product_name,
            ot.product_sku_id ot_product_sku_id,
            ot.product_sku_code ot_product_sku_code,
            ot.product_quantity ot_product_quantity
        FROM
            oms_order o
            LEFT JOIN oms_order_item ot ON o.id = ot.order_id
        WHERE
            o.id = #{orderId}
    </select>

    <select id="getTimeOutOrders" resultMap="orderDetailMap">
        SELECT
            o.id,
            o.order_sn,
            o.coupon_id,
            o.integration,
            o.member_id,
            o.use_integration,
            ot.id               ot_id,
            ot.product_name     ot_product_name,
            ot.product_sku_id   ot_product_sku_id,
            ot.product_sku_code ot_product_sku_code,
            ot.product_quantity ot_product_quantity
        FROM
            oms_order o
            LEFT JOIN oms_order_item ot ON o.id = ot.order_id
        WHERE
            o.status = 0
            AND o.delete_status = 0
            AND o.create_time &lt; date_add(NOW(), INTERVAL -#{minute} MINUTE);
    </select>

    <!--
      支付成功：只有待付款订单能转为待发货，影响行数为 1 才代表本次调用真正完成支付转换。
      重复回调时 status 已不是 0，更新命中 0 行，从而保证只扣减一次库存。
    -->
    <update id="payOrderIfUnpaid">
        UPDATE oms_order
        SET status        = 1,
            pay_type      = #{payType},
            payment_time  = NOW(),
            modify_time   = NOW()
        WHERE id = #{orderId}
          AND status = 0
          AND delete_status = 0
    </update>

    <!-- 取消/超时关闭：只有待付款订单能转为已关闭，保证已支付订单不会被关闭 -->
    <update id="closeOrderIfUnpaid">
        UPDATE oms_order
        SET status      = 4,
            modify_time = NOW()
        WHERE id = #{orderId}
          AND status = 0
          AND delete_status = 0
    </update>

    <!-- 确认收货：只有已发货订单能转为已完成 -->
    <update id="confirmReceiveIfDelivered">
        UPDATE oms_order
        SET status         = 3,
            confirm_status = 1,
            receive_time   = NOW(),
            modify_time    = NOW()
        WHERE id = #{orderId}
          AND status = 2
          AND delete_status = 0
    </update>

    <!-- 锁定库存：可用库存充足才允许锁定，保证 stock - lock_stock 不为负 -->
    <update id="lockSkuStock">
        UPDATE pms_sku_stock
        SET lock_stock = lock_stock + #{quantity}
        WHERE id = #{skuId}
          AND stock - lock_stock &gt;= #{quantity}
    </update>

    <!-- 支付成功扣减库存：真实库存与锁定库存同时充足才允许扣减 -->
    <update id="deductSkuStock">
        UPDATE pms_sku_stock
        SET stock      = stock - #{quantity},
            lock_stock = lock_stock - #{quantity}
        WHERE id = #{skuId}
          AND stock &gt;= #{quantity}
          AND lock_stock &gt;= #{quantity}
    </update>

    <!-- 取消订单释放锁定库存：锁定库存不足时不更新，保证 lock_stock 不为负 -->
    <update id="releaseSkuStockLock">
        UPDATE pms_sku_stock
        SET lock_stock = lock_stock - #{quantity}
        WHERE id = #{skuId}
          AND lock_stock &gt;= #{quantity}
    </update>
</mapper>
