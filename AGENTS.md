# Mall 项目协作规则

> 本文件是当前生效规则。旧版 WorkBuddy/CodeBuddy 流程仅保存在 `AGENTS.legacy.md`，不参与当前任务。

## 1. 项目与工作树

- 项目根目录：`F:\code\mall`
- 后台管理工作树：`F:\code\mall\.worktrees\comment-admin`
- 会员端工作树：`F:\code\mall\.worktrees\comment-member`
- 后台任务只能修改 `comment-admin`，会员端任务只能修改 `comment-member`。
- 不混用工作树，不直接在 `main` 上实现功能。

## 2. 标准工作流

1. 用户提出需求后，主 Agent 先说明业务流程、实现架构、关键文件、接口/数据库影响和验收标准。
2. 主 Agent 将需求拆成后台端和会员端任务，分别生成可直接复制的详细提示词。
3. 用户把提示词发送给对应工作树对话；当前流程不通过 MCP 自动分发编码任务。
4. 用户回传工作树的完成总结后，主 Agent 必须读取实际 `git diff` 和 `git status`，不能只相信总结。
5. 主 Agent 调用只读审查 Agent：
   - `pr_explorer`：审查业务链路、模块依赖、接口和数据流；
   - `reviewer`：审查正确性、安全性、权限、异常、并发和回归风险。
6. 发现问题时，主 Agent 输出包含文件位置、风险和验收要求的返工提示词，由用户转发给对应工作树；返工后重新审查。
7. 审查通过后，运行相关测试、构建或接口验证，再按逻辑单元在对应功能分支创建中文 Git 提交。
8. 提交后先确认功能 worktree 和 `main` 工作区都没有未提交改动，再由主 Agent 在主目录执行本地合并；不在功能 worktree 内切换到 `main`，不把未提交改动带入合并。
9. 本地合并完成后，在 `main` 分支重新运行与本次改动相关的关键验证，并检查合并后的 diff、提交历史和工作区状态。
10. 报告功能分支提交哈希、合并提交哈希、文件清单、验证结果和遗留问题；只有用户明确确认合并结果无误后，才执行 `git push` 上传 GitHub。

## 3. 任务提示词要求

发给工作树对话的提示词必须包含：

- 工作树绝对路径；
- 需求目标和业务流程；
- 允许修改与禁止修改的文件范围；
- 接口、权限和数据库约束；
- 异常处理和验收标准；
- 测试/构建命令；
- 禁止提交 Git、推送远程或扩大任务范围；
- 完成后返回修改文件、关键位置、验证结果和遗留问题。

提示词的 Markdown 格式必须保持可连续复制：

- 每个工作树提示词使用一个完整、连续的代码块，不在代码块中间插入解释文字。
- 提示词内部如果还包含代码、命令或接口示例，外层使用四个反引号（````markdown），内层使用三个反引号（```text、```powershell 等），禁止使用相同层级的嵌套代码围栏。
- 如果客户端可能破坏嵌套围栏，改用缩进代码或纯文本示例，确保用户复制后不会丢失段落、命令和约束。
- 后续返工提示词也遵守同样规则；一个任务一个连续代码块，标题和说明放在代码块外。

## 4. MySQL 操作规则

- `SELECT`、`SHOW`、`DESCRIBE`、`EXPLAIN` 等只读查询可用于分析，但不得输出密码、Token 或密钥。
- `INSERT`、`UPDATE`、`DELETE`、权限修改、数据迁移以及任何 DDL 执行前，必须先说明数据库、表、SQL、影响范围和回滚方式，并取得用户明确确认。
- “继续”“看一下”“没问题”等模糊回复不视为数据库执行授权；需要明确回复“确认执行这份 SQL”。
- `DROP`、`TRUNCATE`、无条件 `UPDATE/DELETE`、核心订单/库存/支付数据修改必须单独确认。
- 子 Agent 可以生成 SQL，但不能默认执行；数据库写操作必须串行，两个工作树不得同时修改数据库。

## 5. Git 与文件安全

- 提交前必须确认不包含 `.env`、`application-dev.yml`、`application-prod.yml`、`node_modules/`、`target/`、`dist/`、数据库备份、密钥和临时迁移文件。
- 使用中文提交信息；一个独立功能或清晰逻辑单元使用一个提交。
- 审查和验证通过后，可以按标准流程在本地合并到 `main`；合并前必须确认 `main` 工作区干净，合并后必须重新验证。
- 未经用户明确确认，不执行 `git push`；推送前必须确认具体远程仓库和分支。
- 功能分支合并后默认保留其提交历史；后续新需求应从最新 `main` 创建新的功能分支/worktree，不继续在已合并的旧功能分支上混合开发。
- 禁止批量删除文件或目录，不得使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse` 或 `rm -rf`。
- 删除文件时只能处理一个明确路径，并先确认影响范围。

## 6. 汇报要求

每次阶段性汇报必须说明：完成内容、修改文件、审查结论、验证命令、测试结果、Git 提交信息、遗留问题和下一步可选方案。

未经实际 diff、只读审查和相关验证，不得宣称功能已完成。
